# Plan — semaine du 2026-07-06 (Lun → Ven)

Pour Raj (UI testing + docking testing) et Matthieu. 5 jours, ça va vite. Priorités : la
**démo (Alex, mardi, LinkedIn)**, **fiabiliser le docking**, **ouvrir les PR**, **la doc**, et
lever le bloqueur hardware. Livrables et critères "done" explicites.

## Contexte / état de départ
- ✅ **Vision/CPU optimisée** : pipeline composé caméra+apriltag (intra-process), détecteur
  15 Hz, Pi à ~45 % CPU. Docking a réussi plusieurs fois. (audit `history/2026-07-03-docking-rework-and-cpu.md`).
- ✅ **Docking en frame-robot** (normale relative au tag, plus la map bancale) : code déployé,
  reste à tester/tuner + résoudre le décalage +4.2°.
- ✅ **Pi retrouvé** : nouveau HW + ancien SSD → **`botshare.local`** (IP DHCP `172.17.17.64`).
- ⚠️ **BLOQUEUR #1 = faux-contact roue droite** (intermittent) → nav/docking aléatoires.
- 5 feature-branches platform-sw prêtes mais **PR pas ouvertes** ; travail d'aujourd'hui pas
  encore committé/PR ; feature voix UI sur `origin/main` à merger avec `real-robot`.

---

## LUNDI — Débloquer + prépa démo + PR "prêtes"
**Objectif : robot fiable ce soir + moitié UI de la démo validée + PR faciles ouvertes.**
- [ ] **Réparer le faux-contact roue droite** (ressouder/reclipser le connecteur moteur droit).
      Vérif : `ros2 topic pub -r10 /cmd_vel_nav … z:0.4` + `ros2 topic echo /debug/right` → rpm
      stable ~9, plus de 0. **Done = spin sur place propre, répétable.** ← le plus important.
- [ ] **Config UI → nouveau Pi** : `.env` + `web/src/shared/constants/index.js` sur `172.17.17.64`.
- [ ] **Branche démo** : merger la voix (`origin/main`) dans `feat/real-robot-integration`.
- [ ] **Clé Claude** dans `ros2/src/openamr_ui_package/.env` (ANTHROPIC_API_KEY).
- [ ] **Test voix→blocs** dans Chrome (backend + micro, sans robot) → valide `/api/voice-plan`.
- [ ] **Créer "Station 4"** (Named Locations) — coords provisoires, à recaler mardi sur la carte.
- [ ] **Committer le code docking du jour** (frame-robot, PWM, EMA, spin floor, nav speedup) sur
      des branches propres.
- [ ] **Ouvrir les 5 PR platform-sw déjà prêtes** vers upstream (gate apriltag, perception,
      nav2-tuning, real-bringup, diagnostics) — 30 min, elles sont prêtes.
- **Done du jour :** robot spin OK, voix→blocs marche à l'écran, 5 PR ouvertes.

## MARDI — DÉMO + 1er docking sur le nouveau Pi
**Objectif : vidéo démo livrée à Alex + robot qui docke sur le nouveau Pi.**
- [ ] **Bring-up complet** sur `botshare.local` : `bringup_composed.launch.py` + 2D Pose Estimate.
      Vérif : 1× chaque nœud, `/scan_filtered` vivant, détections apriltag 15 Hz.
- [ ] **Recaler "Station 4"** sur la vraie carte (conduire le robot au point, lire `/amcl_pose`).
- [ ] **ENREGISTRER LA DÉMO** (30-60 s) : UI → Voice Command → phrase → blocs générés → Enter →
      robot part vers Station 4. Chrome, micro OK, phrase répétée. **→ envoyer à Alex.**
- [ ] Si temps : **1er docking** frame-robot + EMA + PWM sur le robot réparé. Regarder `norm=`,
      l'alignement, l'oscillation.
- **Done du jour :** vidéo démo envoyée ; robot docke au moins 1 fois proprement.

## MERCREDI — Fiabiliser le docking
**Objectif : docking répétable et bien aligné.**
- [ ] **Résoudre le +4.2°** : robot bien perpendiculaire (écart L/R égal) → lire `norm=` →
      ~0 = pas de biais (souci contrôle) / ~+4° = biais caméra → param `normal_yaw_offset`.
- [ ] **Répétabilité** : 5-10 dockings d'affilée, mesurer le taux de réussite + l'alignement final.
- [ ] Si arrive encore de biais : **figer le cap perpendiculaire en NEAR** (odom yaw au handover
      0.70 m, avancer tout droit) au lieu du bearing qui dé-aligne.
- [ ] **Tuning live** (déjà tout réglable à chaud) : lookahead, kp, drive_speed, EMA alpha.
- **Done du jour :** ≥ 8/10 dockings réussis, alignement < ~2°.

## JEUDI — PR du travail neuf + UI réel + doc
**Objectif : le travail d'aujourd'hui en PR, UI branchée sur le robot réel, doc à jour.**
- [ ] **Nouvelles PR** : (a) composition vision intra-process, (b) docking frame-robot + PWM +
      EMA, (c) nav2 planner speedup. Branches propres, sign-off DCO, sans mention Claude.
- [ ] Gérer le **chevauchement** perception ⊂ real-bringup (perception mergée d'abord).
- [ ] **UI real-robot** : panneaux map / caméra / joystick via rosbridge sur `botshare.local` —
      vérifier que ça remonte (piège DDS Cyclone/domain0 déjà documenté).
- [ ] **Documentation** : consolider (`history/2026-07-03-docking-rework-and-cpu.md`, audits CPU, runbook, README UI real-robot),
      mettre à jour les READMEs des packages touchés.
- **Done du jour :** 3 nouvelles PR ouvertes, UI affiche map+caméra du robot réel.

## VENDREDI — Finaliser PR + clôturer + bilan
**Objectif : PR mergeables, discussion #13 close, bilan propre.**
- [ ] **Répondre à review** + ordre de merge des PR (perception → real-bringup → reste).
- [ ] **Bloc "navigate to Station 4" bout-en-bout** depuis l'UI sur le robot réel.
- [ ] **Clôturer la discussion GitHub #13** : commentaire avec liens repos/dossiers, marquer
      done, **lister limites/issues** (faux-contact, +4.2°, nav en espace serré) **+ next plans**.
- [ ] **Bilan** : court récap pour Alex/Raj (ce qui marche, limites, prochaines étapes).
- [ ] Buffer pour le débordement des jours précédents.
- **Done du jour :** PR en cours de merge, #13 close, bilan envoyé.

---

## Fil rouge (à ne pas perdre de vue)
- **Hardware d'abord** (faux-contact) — sinon tout le reste est aléatoire.
- **PR + doc au fil de l'eau**, pas à la fin (lundi = les 5 prêtes ; jeudi = les neuves).
- **Toujours `botshare.local`** (IP DHCP change), commandes complètes copiables, commits sans
  mention Claude + sign-off DCO.
- **Risques** : faux-contact qui récidive ; DHCP qui rechange l'IP ; le +4.2° qui serait un biais
  caméra (calibration) ; la démo qui dépend de Chrome+micro+clé Claude.

## Dépendances externes
- **Clé `ANTHROPIC_API_KEY`** (pour la voix) — à confirmer qu'on en a une valide.
- **Accès repos upstream** pour ouvrir les PR (droits sur openAMRobot).
